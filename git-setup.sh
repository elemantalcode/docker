#!/bin/bash

# Obviously change this to your own credentials

git config --global user.email "elemantalcode@users.noreply.github.com"
git config --global user.name "Elemental Code"
git config --global push.default simple
